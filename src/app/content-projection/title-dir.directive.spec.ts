import { TitleDirDirective } from './title-dir.directive';

describe('TitleDirDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleDirDirective();
    expect(directive).toBeTruthy();
  });
});
